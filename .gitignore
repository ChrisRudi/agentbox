# agentbox — .gitignore

# Sandbox-Template (zu gross fuer Git, wird lokal gebaut)
sandbox/
template.tar.gz

# Paket-Cache (lokal persistent, nicht im Repo)
cache/

# History (lokale Task-Logs)
history/

# Bench-Output (wird pro Run regeneriert, nicht committen)
tools/bench-results.json
bench-results.json

# Windows-Artefakte
*.lnk
Thumbs.db
desktop.ini

# WSL
*.vhdx

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
