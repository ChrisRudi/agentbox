{
  "name": "mcp-handler-template",
  "type": "mcp-handler",
  "description": "Template fuer einen agentbox MCP-Server. Kopiere diesen Ordner unter einem neuen Namen (z.B. 'my-mcp') nach <AI_PROJECTS_ROOT>\\ und trage ihn in config.json mcp_servers ein.",
  "build": {
    "command": "powershell -NoProfile -ExecutionPolicy Bypass -File handler.ps1"
  },
  "build_whitelist": [
    "powershell -NoProfile -ExecutionPolicy Bypass -File handler.ps1"
  ]
}
